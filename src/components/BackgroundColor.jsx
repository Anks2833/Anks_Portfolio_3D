import React, { useEffect, useState } from 'react';

const BackgroundColor = ({ color }) => {
  return null; // No need to render anything, just pass the color to the Canvas
};

export default BackgroundColor;